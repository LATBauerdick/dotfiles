.DS_Store
*CONFLICT*
result
lazy-lock.json
# ...except the neovim one: it is deployed via mkOutOfStoreSymlink in
# users/user/home.nix so lazy writes through to the working tree, which
# only pins plugin versions reproducibly if it is tracked here.
!users/user/vim/lazy-lock.json
vim/autoload/
vim/bundle/
vim/plugged/
vim/plugin/
vim/plugin/packer_compiled.vim
vim/pack/packer/start/packer.nvim/
karabiner/automatic_backups/
karabiner/karabiner.json
workstation/.terraform
.terraform.tfstate.lock.info
terraform.tfstate
terraform.tfstate.backup
